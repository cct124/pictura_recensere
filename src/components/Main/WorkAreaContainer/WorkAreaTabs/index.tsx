import React from "react";
import Style from "./index.modules.scss";
import { classNames } from '@/utils/tool';

export default function WorkAreaTabs() {

  return (
    <div className={classNames(Style.workAreaTabs)}>
    </div>
  )
}