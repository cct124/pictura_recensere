import React from "react";
import Style from "./index.modules.scss";
import { classNames } from '@/utils/tool';

export default function Tools() {
  return (
    <div className={classNames(Style.Tools)}></div>
  )
}